package ordering

import "sort"

// Keys returns map keys in deterministic lexical order.
func Keys[V any](values map[string]V) []string {
	keys := make([]string, 0, len(values))
	for key := range values {
		keys = append(keys, key)
	}
	sort.Strings(keys)
	return keys
}
